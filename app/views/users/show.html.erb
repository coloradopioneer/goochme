<h1><%= @user.username.capitalize %></h1>
<div class="row">
	<div class="col-xs-offset-4 well col-xs-4 center">
		<%= gravatar_for @user %>
	</div>
</div>
<div class="col-xs-offset-4 col-xs-4 center">
	<%= will_paginate @user_games %>
</div>
<%= render 'games/game', obj: @user_games %>
<div class="col-xs-offset-4 col-xs-4 center">
	<%= will_paginate @user_games %>
</div>